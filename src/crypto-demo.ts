import { LitElement, html, css } from 'lit-element';

class CryptoDemo extends LitElement {
  static styles = css``;

  render() {
    return html` <h1>Crypto?</h1> `;
  }
}

customElements.define('crypto-demo', CryptoDemo);
